# Railway / Nixpacks — no Docker required
providers = ["node"]

[variables]
CI = "true"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@10.33.3 --activate",
  # CI: allow fresh publishes (monorepo minimumReleaseAge breaks some builds)
  "sed -i 's/minimumReleaseAge: 1440/minimumReleaseAge: 0/' pnpm-workspace.yaml || true",
  "pnpm install --frozen-lockfile",
]

[phases.build]
cmds = [
  "pnpm --filter @workspace/api-server run build",
]

[start]
cmd = "pnpm --filter @workspace/db run push && node --enable-source-maps artifacts/api-server/dist/index.mjs"
